# Postgres connection string for the gallery database.
DATABASE_URL=postgres://galleryuser:gallerypassword@localhost:5432/gallerydb
# Shared bearer token that clients (Partal's backend) must present.
# Use a long random secret in production, e.g. `openssl rand -hex 32`.
GALLERY_AUTH_TOKEN=local-dev-token
